node_modules/
dist/
web-dist/
coverage/
*.log

# Runtime vault data (canonical data lives on the operator's machine, never in git)
vault/
!vault/.gitkeep

# Connector source material (the operator's private notes/emails) stays local
sources/*
!sources/README.md

# Local env
.env
.env.local
