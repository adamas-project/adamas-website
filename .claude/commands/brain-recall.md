---
description: Answer a question from the second brain (vault first, then accounts)
argument-hint: [your question]
---

Read `brain/CLAUDE.md` first if you haven't this session.

Answer this from the second brain:

$ARGUMENTS

Steps:
1. Search the vault FIRST (`brain/`): grep/glob notes, follow `[[wikilinks]]`,
   check the relevant project's `decisions/` and `index.md`.
2. Answer using what the vault knows. **Cite the note paths** you used.
3. If the vault doesn't have it, say so explicitly, then offer to pull from a
   connected account (`/brain-ingest`) — name which one — but don't reach out
   without saying you're doing so.
4. Keep it tight. If you found a relevant decision, show its id, title, and status.
